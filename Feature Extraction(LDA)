from sklearn.discriminant_analysis import LinearDiscriminantAnalysis
import matplotlib.pyplot as plt
import numpy as np

lda = LinearDiscriminantAnalysis(n_components=1) # Changed n_components from 2 to 1
X, Y = make_classification(n_samples=100, n_features=4, n_informative=2, n_redundant=0, random_state=42)
X_r2 = lda.fit(X, Y).transform(X)

print(lda.explained_variance_ratio_)

colors=['blue','red','green']
vectorizer=np.vectorize(lambda x:colors[x%len(colors)])

plt.scatter(X_r2[:,0], np.zeros_like(X_r2[:,0]), c=vectorizer(Y)) # Adjusted for 1 component
plt.show()
